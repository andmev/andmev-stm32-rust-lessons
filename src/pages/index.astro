---
import HomeTemplate from '@/components/HomeTemplate.astro';

// Apple-style default: show English content at `/` (no redirect, no auto-detection).
const lang = 'en';
const lessonLabel = 'Lesson';
---

<HomeTemplate lang={lang} lessonLabel={lessonLabel} />
